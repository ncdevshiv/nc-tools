{
  "generatedFrom": "rust/nct-mcp (build_kernel)",
  "toolCount": 57,
  "tools": [
    {
      "name": "batch.execute",
      "description": "Run up to 25 kernel tool calls in ONE round-trip: [{tool, args}]. Each sub-call is individually executed and journaled; failures do not abort the batch. Use for independent multi-step work.",
      "inputSchema": {
        "type": "object",
        "required": [
          "calls"
        ],
        "properties": {
          "calls": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "tool"
              ],
              "properties": {
                "args": {
                  "type": "object"
                },
                "tool": {
                  "type": "string"
                }
              },
              "additionalProperties": false
            },
            "maxItems": 25,
            "minItems": 1
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "code.symbols",
      "description": "Extract code symbols (fns, structs, enums, traits, impls, mods, classes, interfaces, defs) with line numbers - a fast lexical scan, not a full parser. Languages: Rust, JS/TS/TSX/JSX, Python.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "glob": {
            "type": "string"
          },
          "kinds": {
            "description": "Only return these kinds, e.g. fn, struct",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "maxResults": {
            "minimum": 1,
            "type": "integer",
            "maximum": 5000
          },
          "path": {
            "description": "Path - relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "env.get",
      "description": "Read an environment variable (session override wins over host).",
      "inputSchema": {
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "env.list",
      "description": "List session environment overrides.",
      "inputSchema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {},
        "required": []
      }
    },
    {
      "name": "env.set",
      "description": "Set a session environment variable; all subsequent proc.* calls inherit it.",
      "inputSchema": {
        "type": "object",
        "required": [
          "name",
          "value"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "value": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "fs.append",
      "description": "Append content to a file (creates it and parent dirs if missing).",
      "inputSchema": {
        "type": "object",
        "required": [
          "content",
          "path"
        ],
        "properties": {
          "content": {
            "type": "string"
          },
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "fs.copy",
      "description": "Copy a file or directory (recursive default). Overwrites existing destinations (journal records the event).",
      "inputSchema": {
        "type": "object",
        "required": [
          "from",
          "to"
        ],
        "properties": {
          "from": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          },
          "recursive": {
            "type": "boolean"
          },
          "to": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "fs.delete",
      "description": "Delete a file or directory (needs recursive for dirs).",
      "inputSchema": {
        "type": "object",
        "required": [
          "path"
        ],
        "properties": {
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          },
          "recursive": {
            "type": "boolean"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "fs.list",
      "description": "List directory entries.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          },
          "recursive": {
            "type": "boolean"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "fs.mkdir",
      "description": "Create a directory.",
      "inputSchema": {
        "type": "object",
        "required": [
          "path"
        ],
        "properties": {
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          },
          "recursive": {
            "type": "boolean"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "fs.move",
      "description": "Move/rename a file or directory.",
      "inputSchema": {
        "type": "object",
        "required": [
          "from",
          "to"
        ],
        "properties": {
          "from": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          },
          "to": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "fs.read",
      "description": "Read a text file with line numbers. Supports offset/limit paging. Returns a digest (hash+mtime) — use it to avoid re-reading unchanged files.",
      "inputSchema": {
        "type": "object",
        "required": [
          "path"
        ],
        "properties": {
          "limit": {
            "minimum": 1,
            "type": "integer"
          },
          "offset": {
            "description": "1-based line to start from",
            "minimum": 1,
            "type": "integer"
          },
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "fs.readMany",
      "description": "Read up to 50 files in ONE call. Each item returns ok/content or a structured error. Prefer this over N separate fs.read calls.",
      "inputSchema": {
        "type": "object",
        "required": [
          "paths"
        ],
        "properties": {
          "limit": {
            "minimum": 1,
            "type": "integer"
          },
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "maxItems": 50,
            "minItems": 1
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "fs.readRange",
      "description": "Read a BYTE RANGE of a file without loading it whole - for huge files and logs. Returns the decoded window, byte offsets (chain via nextByteOffset), the 1-based line number where the window starts, and eof. Use fs.read for line-based paging of normal files.",
      "inputSchema": {
        "type": "object",
        "required": [
          "path"
        ],
        "properties": {
          "byteOffset": {
            "description": "Byte offset to start from (0 = file start)",
            "type": "integer"
          },
          "maxBytes": {
            "description": "Window size in bytes (default 65536, max 1048576)",
            "minimum": 1,
            "type": "integer",
            "maximum": 1048576
          },
          "path": {
            "description": "Path - relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "fs.stat",
      "description": "Stat a path (exists, type, size).",
      "inputSchema": {
        "type": "object",
        "required": [
          "path"
        ],
        "properties": {
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "fs.tree",
      "description": "Render a bounded directory tree in one call: depth-capped, entry-capped, skips .git/node_modules/target/dist/.nc-tools. Returns structured entries (path/type/size/depth) plus an ASCII rendering. Directories sort first.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "depth": {
            "description": "Max depth (default 4, max 12)",
            "minimum": 1,
            "type": "integer",
            "maximum": 12
          },
          "maxEntries": {
            "description": "Max entries (default 500, max 5000)",
            "minimum": 1,
            "type": "integer",
            "maximum": 5000
          },
          "path": {
            "description": "Path - relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "fs.write",
      "description": "Write a file (creates parent dirs). Returns created/overwrote.",
      "inputSchema": {
        "type": "object",
        "required": [
          "content",
          "path"
        ],
        "properties": {
          "content": {
            "type": "string"
          },
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "fs.writeMany",
      "description": "Write up to 50 files in ONE call: [{path, content}]. Per-item ok/error results.",
      "inputSchema": {
        "type": "object",
        "required": [
          "files"
        ],
        "properties": {
          "files": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "content",
                "path"
              ],
              "properties": {
                "content": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                }
              },
              "additionalProperties": false
            },
            "maxItems": 50,
            "minItems": 1
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "git.add",
      "description": "Stage files.",
      "inputSchema": {
        "type": "object",
        "required": [
          "paths"
        ],
        "properties": {
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1
          },
          "repo": {
            "description": "Repo directory (default: base dir)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "git.blame",
      "description": "Per-line blame for a file: commit sha, author, timestamp, content, line number. Uses git blame --line-porcelain.",
      "inputSchema": {
        "type": "object",
        "required": [
          "path"
        ],
        "properties": {
          "lineEnd": {
            "type": "integer"
          },
          "lineStart": {
            "type": "integer"
          },
          "path": {
            "description": "File to blame — relative to the repo base dir, or absolute",
            "type": "string"
          },
          "repo": {
            "description": "Repo directory (default: base dir)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "git.branch",
      "description": "List branches (no args) or create one ({name}).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "repo": {
            "description": "Repo directory (default: base dir)",
            "type": "string"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "git.checkout",
      "description": "Switch branches (or create with create=true).",
      "inputSchema": {
        "type": "object",
        "required": [
          "branch"
        ],
        "properties": {
          "branch": {
            "type": "string"
          },
          "create": {
            "type": "boolean"
          },
          "repo": {
            "description": "Repo directory (default: base dir)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "git.commit",
      "description": "Commit staged changes.",
      "inputSchema": {
        "type": "object",
        "required": [
          "message"
        ],
        "properties": {
          "message": {
            "type": "string"
          },
          "repo": {
            "description": "Repo directory (default: base dir)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "git.diff",
      "description": "Unified diff of unstaged changes.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          },
          "repo": {
            "description": "Repo directory (default: base dir)",
            "type": "string"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "git.log",
      "description": "Recent commits.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "maxCount": {
            "minimum": 1,
            "type": "integer",
            "maximum": 200
          },
          "repo": {
            "description": "Repo directory (default: base dir)",
            "type": "string"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "git.pull",
      "description": "Pull from a remote (ff-only by default — no surprise merge commits).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "branch": {
            "type": "string"
          },
          "ffOnly": {
            "type": "boolean"
          },
          "remote": {
            "type": "string"
          },
          "repo": {
            "description": "Repo directory (default: base dir)",
            "type": "string"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "git.push",
      "description": "Push to a remote (optionally sets upstream).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "branch": {
            "type": "string"
          },
          "remote": {
            "type": "string"
          },
          "repo": {
            "description": "Repo directory (default: base dir)",
            "type": "string"
          },
          "setUpstream": {
            "type": "boolean"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "git.status",
      "description": "Git status: repo path, branch, head, changed files.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "repo": {
            "description": "Repo directory (default: base dir)",
            "type": "string"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "net.http",
      "description": "Perform an HTTP request. Returns status, headers, body (capped), duration. Replaces curl/wget.",
      "inputSchema": {
        "type": "object",
        "required": [
          "url"
        ],
        "properties": {
          "body": {
            "type": "string"
          },
          "headers": {
            "type": "object"
          },
          "method": {
            "type": "string",
            "enum": [
              "GET",
              "POST",
              "PUT",
              "PATCH",
              "DELETE",
              "HEAD"
            ]
          },
          "timeoutMs": {
            "minimum": 100,
            "type": "integer",
            "maximum": 120000
          },
          "url": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "net.probePort",
      "description": "Check whether a TCP port is open. Replaces nc/netstat probing.",
      "inputSchema": {
        "type": "object",
        "required": [
          "port"
        ],
        "properties": {
          "host": {
            "type": "string"
          },
          "port": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65535
          },
          "timeoutMs": {
            "minimum": 100,
            "type": "integer",
            "maximum": 10000
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "patch.apply",
      "description": "Apply exact-match search/replace edits to a file. Each edit: {oldText, newText, expectedCount?}. Fails with structured hints (occurrence counts, nearest candidate lines) if not found or ambiguous.",
      "inputSchema": {
        "type": "object",
        "required": [
          "edits",
          "path"
        ],
        "properties": {
          "edits": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "newText",
                "oldText"
              ],
              "properties": {
                "expectedCount": {
                  "minimum": 1,
                  "type": "integer"
                },
                "newText": {
                  "type": "string"
                },
                "oldText": {
                  "type": "string"
                }
              },
              "additionalProperties": false
            },
            "minItems": 1
          },
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "patch.applyMany",
      "description": "Apply patch edits to up to 20 files in ONE call: [{path, edits:[{oldText,newText,expectedCount?}]}]. Per-file ok/error results.",
      "inputSchema": {
        "type": "object",
        "required": [
          "edits"
        ],
        "properties": {
          "edits": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "edits",
                "path"
              ],
              "properties": {
                "edits": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "required": [
                      "newText",
                      "oldText"
                    ],
                    "properties": {
                      "expectedCount": {
                        "minimum": 1,
                        "type": "integer"
                      },
                      "newText": {
                        "type": "string"
                      },
                      "oldText": {
                        "type": "string"
                      }
                    },
                    "additionalProperties": false
                  },
                  "minItems": 1
                },
                "path": {
                  "type": "string"
                }
              },
              "additionalProperties": false
            },
            "maxItems": 20,
            "minItems": 1
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "pkg.add",
      "description": "Install packages (npm or pip). Structured result; ERR_NETWORK hint if registry unreachable.",
      "inputSchema": {
        "type": "object",
        "required": [
          "names"
        ],
        "properties": {
          "dev": {
            "type": "boolean"
          },
          "dir": {
            "description": "Directory (default: base dir)",
            "type": "string"
          },
          "manager": {
            "type": "string",
            "enum": [
              "npm",
              "pip"
            ]
          },
          "names": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1
          },
          "timeoutMs": {
            "minimum": 1000,
            "type": "integer"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "pkg.list",
      "description": "List installed packages for npm (from package.json) or pip.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dir": {
            "description": "Directory (default: base dir)",
            "type": "string"
          },
          "manager": {
            "type": "string",
            "enum": [
              "npm",
              "pip"
            ]
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "pkg.runScript",
      "description": "Run an npm script with typed args. Returns exit code + captured output.",
      "inputSchema": {
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "args": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "dir": {
            "description": "Directory (default: base dir)",
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "timeoutMs": {
            "minimum": 1000,
            "type": "integer"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "pkg.scripts",
      "description": "List npm scripts defined in package.json.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dir": {
            "description": "Directory (default: base dir)",
            "type": "string"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "proc.kill",
      "description": "Kill a process by PID (system process, not just managed handles).",
      "inputSchema": {
        "type": "object",
        "required": [
          "pid"
        ],
        "properties": {
          "force": {
            "type": "boolean"
          },
          "pid": {
            "type": "integer",
            "minimum": 1
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "proc.list",
      "description": "List the OS process table (tasklist/ps). Optional substring filter by process name.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filter": {
            "type": "string"
          },
          "maxResults": {
            "minimum": 1,
            "type": "integer",
            "maximum": 2000
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "proc.readOutput",
      "description": "Read recent output (stdout+stderr merged) of a background process.",
      "inputSchema": {
        "type": "object",
        "required": [
          "handleId"
        ],
        "properties": {
          "fromEnd": {
            "minimum": 100,
            "type": "integer",
            "maximum": 100000
          },
          "handleId": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "proc.runScript",
      "description": "Run an interpreted script (js/python/shell/powershell/batch) with a hard timeout. Provide a file path OR inline source. Captures stdout/stderr + exit code.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "args": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "cwd": {
            "description": "Working directory (default: base dir)",
            "type": "string"
          },
          "language": {
            "description": "Language: js|python|shell|powershell|batch (inferred from path extension if omitted)",
            "type": "string"
          },
          "path": {
            "description": "Script file - relative to the base dir, or absolute",
            "type": "string"
          },
          "source": {
            "description": "Inline source (used when no path is given)",
            "type": "string"
          },
          "timeoutMs": {
            "minimum": 100,
            "type": "integer",
            "maximum": 600000
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "proc.spawn",
      "description": "Run a program with typed argv (no shell). Captures stdout/stderr, exit code, hard timeout.",
      "inputSchema": {
        "type": "object",
        "required": [
          "cmd"
        ],
        "properties": {
          "args": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "cmd": {
            "type": "string"
          },
          "cwd": {
            "description": "Working dir (absolute allowed; default base dir)",
            "type": "string"
          },
          "timeoutMs": {
            "minimum": 100,
            "type": "integer",
            "maximum": 600000
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "proc.start",
      "description": "Start a LONG-RUNNING background process (server, watcher). Returns a handleId. NOT for one-shot commands — use proc.spawn for those.",
      "inputSchema": {
        "type": "object",
        "required": [
          "cmd"
        ],
        "properties": {
          "args": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "cmd": {
            "type": "string"
          },
          "cwd": {
            "description": "Working dir (absolute allowed; default base dir)",
            "type": "string"
          },
          "maxDurationMs": {
            "minimum": 1000,
            "type": "integer",
            "maximum": 3600000
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "proc.status",
      "description": "Status of a background process handle: running, exitCode, outputBytes, uptime.",
      "inputSchema": {
        "type": "object",
        "required": [
          "handleId"
        ],
        "properties": {
          "handleId": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "proc.stop",
      "description": "Stop a background process by handle.",
      "inputSchema": {
        "type": "object",
        "required": [
          "handleId"
        ],
        "properties": {
          "force": {
            "type": "boolean"
          },
          "handleId": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "proc.watch",
      "description": "Watch a file/dir and re-run a long-lived command whenever the tree changes (auto-rebuild / dev server). Returns a handleId managed by proc.status/readOutput/stop.",
      "inputSchema": {
        "type": "object",
        "required": [
          "cmd",
          "path"
        ],
        "properties": {
          "args": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "cmd": {
            "description": "Command to keep running (a server / rebuild loop)",
            "type": "string"
          },
          "cwd": {
            "description": "Working directory (default: base dir)",
            "type": "string"
          },
          "intervalMs": {
            "minimum": 100,
            "type": "integer",
            "maximum": 10000
          },
          "maxDurationMs": {
            "minimum": 1000,
            "type": "integer",
            "maximum": 3600000
          },
          "path": {
            "description": "File or directory to watch (recursive)",
            "type": "string"
          },
          "timeoutMs": {
            "minimum": 100,
            "type": "integer",
            "maximum": 600000
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search.files",
      "description": "Find files by glob pattern (e.g. \"**/*.test.mjs\").",
      "inputSchema": {
        "type": "object",
        "required": [
          "pattern"
        ],
        "properties": {
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          },
          "pattern": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search.grep",
      "description": "Regex search across files. Returns file/line/text matches.",
      "inputSchema": {
        "type": "object",
        "required": [
          "pattern"
        ],
        "properties": {
          "glob": {
            "type": "string"
          },
          "maxResults": {
            "minimum": 1,
            "type": "integer",
            "maximum": 1000
          },
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          },
          "pattern": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search.replace",
      "description": "Regex search/replace across files. dryRun=true (default) reports per-file match counts without writing; dryRun=false rewrites files (run sys.snapshot first for rollback). Supports $0-$9 capture backrefs ($$ = literal $). Skips binaries and .git/node_modules/target/dist/build/.nc-tools.",
      "inputSchema": {
        "type": "object",
        "required": [
          "pattern",
          "replacement"
        ],
        "properties": {
          "dryRun": {
            "description": "true (default) = report only; false = write the changes",
            "type": "boolean"
          },
          "glob": {
            "type": "string"
          },
          "maxFiles": {
            "minimum": 1,
            "type": "integer",
            "maximum": 2000
          },
          "path": {
            "description": "Root of the search (default: base dir)",
            "type": "string"
          },
          "pattern": {
            "type": "string"
          },
          "replacement": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search.semantic",
      "description": "Neural-semantic search: ranks files by meaning-match to a natural-language query. Uses a local MiniLM transformer (all-MiniLM-L6-v2) — no API calls. Returns file, score, bytes.",
      "inputSchema": {
        "type": "object",
        "required": [
          "query"
        ],
        "properties": {
          "path": {
            "description": "Path — relative to the base dir, or absolute (any location allowed)",
            "type": "string"
          },
          "query": {
            "type": "string"
          },
          "topK": {
            "minimum": 1,
            "type": "integer",
            "maximum": 100
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "sys.doctor",
      "description": "Self-diagnostics: registered tools, config limits, session env, journal stats. deep=true also probes runtime (workspace writable, cargo on PATH).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "deep": {
            "description": "Include runtime probes (writable root, cargo available)",
            "type": "boolean"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "sys.journal",
      "description": "Read the session journal (your own tool-call trail).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "lastN": {
            "minimum": 1,
            "type": "integer",
            "maximum": 1000
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "sys.listSnapshots",
      "description": "List available snapshots.",
      "inputSchema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {},
        "required": []
      }
    },
    {
      "name": "sys.rollback",
      "description": "Restore the workspace to a snapshot: restores manifest files, removes files created after the snapshot.",
      "inputSchema": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "sys.snapshot",
      "description": "Capture a workspace snapshot (files + dirs, excludes .git/node_modules/.nc-tools). Returns an id usable with sys.rollback. Use before risky edits so you can undo.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "label": {
            "type": "string"
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "sys.workspace",
      "description": "Workspace info: root path, platform.",
      "inputSchema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {},
        "required": []
      }
    },
    {
      "name": "test.run",
      "description": "Run a test suite and get STRUCTURED results: passed/failed counts, failing test names + messages. Frameworks: node (node:test), pytest.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "framework": {
            "type": "string",
            "enum": [
              "node",
              "pytest",
              "cargo"
            ]
          },
          "path": {
            "description": "Test file/dir path (absolute allowed); default = standard patterns in base dir",
            "type": "string"
          },
          "timeoutMs": {
            "minimum": 1000,
            "type": "integer",
            "maximum": 600000
          }
        },
        "additionalProperties": false,
        "required": []
      }
    },
    {
      "name": "text.diff",
      "description": "Unified diff between two files (or a file and a string). Returns a git-style @@ hunk diff plus parseable counts. Paths are relative to the base dir or absolute.",
      "inputSchema": {
        "type": "object",
        "required": [
          "path",
          "path2"
        ],
        "properties": {
          "context": {
            "description": "Context lines per side (default 3, max 50)",
            "maximum": 50,
            "type": "integer"
          },
          "path": {
            "description": "Left file - relative to the base dir, or absolute",
            "type": "string"
          },
          "path2": {
            "description": "Right file - relative to the base dir, or absolute",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    }
  ]
}
