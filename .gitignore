# build artifacts
/target/
node_modules/

# local machine config & secrets (never commit)
.env
.env.*
!.env.example

# local run artifacts (results are reproducible; transcripts stay local)
bench/results/
benchmark/results/
.nc-tools/
.*.log
.test.out.log

# golden regen temp artifact
conformance/golden/.golden.tmp.json
